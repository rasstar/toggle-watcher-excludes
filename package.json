{
  "name": "toggle-watcher-excludes",
  "displayName": "Toggle Watcher Excludes",
  "description": "Toggle files.watcherExclude between global and workspace settings",
  "version": "0.0.2",
  "engines": {
    "vscode": "^1.50.0"
  },
  "activationEvents": ["onCommand:extension.toggleWatcherExclude"],
  "main": "extension.js",
  "contributes": {
    "commands": [
      {
        "command": "extension.toggleWatcherExclude",
        "title": "Toggle: Watcher Excludes"
      }
    ]
  },
  "categories": ["Other"]
}
